/**
 * Pickup abstract class - Jack Klug
 * abstract class that defines all entities that will be picked up off the ground
 */
abstract class Pickup extends Entity{
    public Pickup(){
        super(desc);
    }
}